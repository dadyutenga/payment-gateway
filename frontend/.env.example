# Copy this to .env and fill in your own values.

# Where the Go backend is deployed.
# In dev this defaults to http://localhost:8080 if unset.
VITE_API_BASE_URL=https://your-payments-api.example.com
